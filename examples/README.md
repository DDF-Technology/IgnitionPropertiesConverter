# Esempi sintetici

I due file `.properties` non contengono dati cliente. Possono essere convertiti insieme in Excel per provare il flusso completo e verificare escape, continuazioni e valori che iniziano con `=`.

Il pacchetto Windows include anche `Translations-example.xlsx`, generato automaticamente dagli stessi file durante la build.
